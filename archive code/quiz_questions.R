#Love Lines Quiz Question
#loveLines <- length(grep("love", twitterData))
#hateLines <- length(grep("hate", twitterData))
#loveByHate <- loveLines/hateLines

#Biostat Quiz Question
#biostatsTweet <- grep("biostat", twitterData, value=TRUE)

#Kickboxing Tweets Quiz Question
#kickboxingTweets <- length(grep("A computer once beat me at chess, but it was no match for me at kickboxing",
#                                twitterData))